# prettier-config

> Shared [Prettier](https://prettier.io) config for my projects.

## Install

```
$ npm install --save-dev @involvex/prettier-config
```

## Usage

Add this to your `package.json`:

```json
{
	"prettier": "@involvex/prettier-config"
}
```
